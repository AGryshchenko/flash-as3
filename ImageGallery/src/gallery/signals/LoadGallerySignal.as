package gallery.signals {
    import org.osflash.signals.Signal;

    public class LoadGallerySignal extends Signal {
        public function LoadGallerySignal() {
            super();
        }
    }
}